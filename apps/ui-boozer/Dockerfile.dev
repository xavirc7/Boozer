FROM node:22-alpine

WORKDIR /app

# Compose mounts the source and keeps Linux dependencies in a separate volume.
# Install on startup so package-lock.json changes are picked up on restart.
CMD ["sh", "-c", "npm ci && exec npm run dev -- --host 0.0.0.0 --port 80 --strictPort"]
